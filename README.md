# Liberty Medical (Next.js)

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Notes

- The original `index.html` is left in place as a reference.
- Global styling lives in `app/globals.css` (ported from the original `<style>` block) and Tailwind is enabled for future component styling.

